---
interface Props { label: string; title: string; }
const { label, title } = Astro.props;
---

<div class="mb-5">
  <p class="text-xs font-semibold uppercase tracking-[0.2em] text-primary">{label}</p>
  <h2 class="mt-2 text-xl font-bold">{title}</h2>
</div>
