# Public URL of your authorised NeteaseCloudMusicApiEnhanced deployment.
# The site only sends public GET requests for the player and profile card; never put cookies here.
# Leave empty to use the bundled audio and profile snapshot.
PUBLIC_NCM_API_BASE=

# 网易云公开个人主页 UID。默认已设置为 Bad0RANG3 的 UID。
PUBLIC_NCM_USER_ID=1864136351
